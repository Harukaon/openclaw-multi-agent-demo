# Copy to .env on the Platform host; never commit the filled file.
PLATFORM_IMAGE=ghcr.io/harukaon/feedmob-openclaw-group-platform:latest
# Optional controlled-broadcast tuning.
BROADCAST_MAX_AGENT_REPLIES=12
BROADCAST_ACK_TIMEOUT_MS=120000
BROADCAST_SETTLE_MS=2000
